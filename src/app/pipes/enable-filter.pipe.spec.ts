import { EnableFilterPipe } from './enable-filter.pipe';

describe('EnableFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new EnableFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
