import { ArchiveFilterPipe } from './archive-filter.pipe';

describe('ArchiveFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ArchiveFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
