import { TimeSpliterPipe } from './time-spliter.pipe';

describe('TimeSpliterPipe', () => {
  it('create an instance', () => {
    const pipe = new TimeSpliterPipe();
    expect(pipe).toBeTruthy();
  });
});
